

function FlipItGame( board_id, start_btn_id, flip_btn_id, score_id ){

  var board = $("#game_board")[0];

  //setup board
  var c = board.getContext("2d");
  c.fillStyle = "#111111";
  c.fillRect(0,0,800,200);

  //setup buttons
}

